# vmauth substitutes %{ENV} at startup. The password must not contain quotes.
users:
  - username: "%{VMAUTH_USERNAME}"
    password: "%{VMAUTH_PASSWORD}"
    url_map:
      - src_paths:
          - /api/v1/write
        url_prefix: http://victoriametrics:8428
